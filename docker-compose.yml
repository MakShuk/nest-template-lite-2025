# docker-compose.yml
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5686:5686"
    environment:
      - PORT=5686
      - NODE_ENV=production
    labels:
      app: nestjs
      env: test
      team: backend
      service: nest-template-lite-2025
    restart: unless-stopped
