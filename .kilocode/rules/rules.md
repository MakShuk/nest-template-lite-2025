{
    "version": "1.0.0",
    "language": "ru",
    "scope": [
        "nestjs",
        "typescript"
    ],
    "corePrinciples": [
        {
            "id": "process-depth-reasoning",
            "title": "Глубокая логика и доказуемость",
            "description": "Никогда не использовать поверхностную логику. Если решение кажется простым — копать глубже до неопровержимой логики.",
            "rules": [
                "Перед изменениями описывай причины, риски и альтернативы.",
                "Для любого изменения формулируй критерии готовности (Definition of Done).",
                "Если есть неопределенность — задавай уточняющие вопросы до реализации."
            ]
        },
        {
            "id": "process-plan-and-finish",
            "title": "Планируй и доводи до конца",
            "description": "Если есть план — убедись, что все пункты выполнены; не останавливайся, пока все не сделано.",
            "rules": [
                "Любая задача начинается с плана изменений (что/где/почему).",
                "После реализации выполняй полный набор проверок (линт, типы, тесты).",
                "Если часть работ требует действий пользователя (запуск сервера/проверка интеграции) — явно проси это и перечисляй, что проверить и какие данные прислать."
            ]
        },
        {
            "id": "process-linking-check",
            "title": "Проверка связывания",
            "description": "Запускать проверки, которые гарантируют корректность связывания модулей/зависимостей/типов, и не останавливаться, пока не будет без проблем.",
            "rules": [
                "В NestJS проверять корректность DI: экспорт/импорт модулей, провайдеры, токены, circular deps.",
                "Проверять сборку TypeScript и отсутствие неиспользуемых/битых импортов.",
                "Фиксировать чек-лист команд, которые должны проходить локально и в CI."
            ]
        },
        {
            "id": "yagni",
            "title": "YAGNI",
            "description": "You Aren't Gonna Need It — не добавляй функциональность, пока она не понадобится.",
            "rules": [
                "Никаких преждевременных абстракций.",
                "Никаких решений «на всякий случай».",
                "Реализуй только то, что требуется сейчас.",
                "Если сомневаешься — не добавляй."
            ]
        }
    ],
    "architecture": [
        {
            "id": "nestjs-classes-minimal",
            "title": "Классы только там, где требует NestJS",
            "description": "NestJS архитектурно основан на классах (controllers/providers/modules). Сохраняем их тонкими, а бизнес-логику держим в чистых функциях.",
            "rules": [
                "Контроллеры — тонкие: валидация входа, вызов use-case, маппинг ответа.",
                "Провайдеры (services) — оболочки для DI и инфраструктуры; доменная логика в функциях/модулях без состояния.",
                "Не раздувать Base-классы и наследование; если можно — композиция и функции."
            ]
        },
        {
            "id": "module-boundaries",
            "title": "Четкие границы модулей",
            "description": "Каждый модуль описывает свою область ответственности и экспортирует только необходимое.",
            "rules": [
                "Не экспортировать лишние провайдеры «на будущее».",
                "Каждый модуль имеет явный публичный API (exports) и скрытую реализацию.",
                "Избегать циклических зависимостей между модулями; при необходимости — выделять общий модуль."
            ]
        }
    ],
    "apiContract": [
        {
            "id": "api-clear-contract",
            "title": "Четкий контракт API",
            "description": "Контракт должен быть однозначным, валидируемым и легко тестируемым.",
            "rules": [
                "DTO/входные структуры валидировать Zod-схемами (или через pipe-адаптер на Zod).",
                "Ответы документировать (OpenAPI/Swagger) и стабилизировать формой объектов.",
                "Ошибки бизнес-уровня не выбрасывать как исключения; возвращать ServiceResponse."
            ]
        }
    ],
    "errorHandling": [
        {
            "id": "service-response-contract",
            "title": "Result/Either-подобный контракт (ServiceResponse)",
            "description": "Все публичные методы доменного/сервисного слоя возвращают ServiceResponse<T> вместо исключений для бизнес-ошибок. Это упрощает композицию и делает ошибки явными.",
            "typeContracts": {
                "ServiceError": {
                    "shape": {
                        "code": "string",
                        "message": "string",
                        "details": "unknown | undefined"
                    }
                },
                "ServiceResponse": "type ServiceResponse<T> = { success: true; result: T } | { success: false; error: ServiceError }"
            },
            "rules": [
                "Для бизнес-ошибок: возвращать { success: false, error }.",
                "Исключения допускаются только для truly-exceptional ситуаций: падение инфраструктуры, баг, недоступность внешнего сервиса — и должны быть перехвачены глобальными фильтрами NestJS.",
                "Избегать try/catch в бизнес-логике; предпочитать явные проверки результата, guard clauses и функции-адаптеры, возвращающие ServiceResponse."
            ],
            "examples": [
                "function ensureFound<T>(value: T | null, error: { code: string; message: string }): { success: true; result: T } | { success: false; error: { code: string; message: string } } { if (!value) return { success: false, error }; return { success: true, result: value }; }",
                "function mapResult<A, B>(res: { success: true; result: A } | { success: false; error: any }, fn: (a: A) => B) { if (!res.success) return res; return { success: true, result: fn(res.result) }; }"
            ]
        }
    ],
    "codingStyle": [
        {
            "id": "functional-declarative",
            "title": "Функциональный и декларативный стиль",
            "description": "Предпочитать чистые функции и композицию. Классы избегать вне мест, где это диктует NestJS.",
            "rules": [
                "Писать чистые функции ключевым словом function.",
                "Сайд-эффекты изолировать на границах (адаптеры, репозитории, клиенты).",
                "Дублирование заменять небольшими утилитами/комбинаторами для ServiceResponse."
            ]
        },
        {
            "id": "roro",
            "title": "RORO (Receive an Object, Return an Object)",
            "description": "Функции принимают один объект параметров и возвращают объект результата; это повышает читаемость и снижает риск ошибок при расширении сигнатур.",
            "rules": [
                "Публичные функции/use-cases принимают один объект аргументов.",
                "Возврат — объект (часто ServiceResponse<T>)."
            ]
        },
        {
            "id": "guard-clauses",
            "title": "Guard clauses и if-return",
            "description": "Защитные проверки — ранние, happy-path — последним; избегать лишних else.",
            "rules": [
                "Предусловия и невалидные состояния обрабатываются ранним return.",
                "Happy path размещать последним в функции.",
                "Для простых условий использовать однострочный синтаксис: if (cond) doSomething()."
            ]
        },
        {
            "id": "naming",
            "title": "Именование",
            "description": "Имена должны отражать смысл и состояние.",
            "rules": [
                "Переменные состояния с вспомогательными глаголами: isLoading, hasError, canRetry.",
                "Функции: глагол + объект (getUser, buildReport, ensureAccess).",
                "Папки — kebab-case: modules/order-sync, infra/http-client."
            ]
        },
        {
            "id": "exports",
            "title": "Экспорты",
            "description": "Предпочитать именованные экспорты для функций/модулей.",
            "rules": [
                "Не использовать default export без необходимости.",
                "Держать публичные экспорты рядом (index.ts) только когда это реально упрощает импорт."
            ]
        }
    ],
    "typescriptAndZod": [
        {
            "id": "ts-everywhere",
            "title": "TypeScript везде",
            "description": "Весь код проекта — TypeScript, строгие настройки компилятора.",
            "rules": [
                "Включить strict-режим и не игнорировать ошибки типизации.",
                "Формы объектов описывать интерфейсами, а не type (кроме юнионов/утилитарных типов)."
            ]
        },
        {
            "id": "zod-validation",
            "title": "Zod для схем и вывода типов",
            "description": "Zod используется как единый источник правды для валидации и типов.",
            "rules": [
                "DTO/входы валидировать Zod-схемой на границе (controller/pipe).",
                "Типы выводить через z.infer для синхронизации схемы и типов."
            ]
        },
        {
            "id": "no-enums",
            "title": "Избегать enum",
            "description": "Enum усложняют сериализацию/совместимость; предпочитать литеральные типы или мапы.",
            "rules": [
                "Использовать string literal unions.",
                "Использовать const-объекты/Record для отображений."
            ]
        }
    ],
    "projectStructure": [
        {
            "id": "file-structure",
            "title": "Структура файлов",
            "description": "Файлы структурировать вокруг модуля/фичи: экспортируемое API, помощники, типы, статическое содержимое.",
            "rules": [
                "Фича-модуль: controller, module, providers, use-cases, schemas, mappers.",
                "Типы и схемы — рядом с использованием, а не в глобальной свалке.",
                "Инфраструктура отдельно: infra/db, infra/http, infra/logger."
            ]
        }
    ],
    "qualityBar": [
        {
            "id": "production-ready",
            "title": "Production-ready качество",
            "description": "Код должен быть оптимизированным, уникальным, готовым к продакшену и опираться на существующие библиотеки.",
            "rules": [
                "Не изобретать велосипеды: использовать проверенные пакеты (валидация, логирование, конфиги).",
                "Оптимизировать только там, где есть измеримая проблема (YAGNI + метрики).",
                "Каждое изменение сопровождается проверяемыми критериями качества (типы/тесты/линт)."
            ]
        }
    ],
    "workflow": [
        {
            "id": "commands-to-verify",
            "title": "Команды проверок (локально/CI)",
            "description": "Набор команд, которые должны проходить без ошибок перед мерджем.",
            "commands": [
                "pnpm lint",
                "pnpm test",
                "pnpm build",
                "pnpm typecheck"
            ],
            "rules": [
                "Если используется иной пакетный менеджер — закрепить эквивалентные команды.",
                "Проверка связывания включает сборку и прогон тестов модулей/DI."
            ]
        },
        {
            "id": "when-user-must-run",
            "title": "Когда просить пользователя запустить сервер",
            "description": "Если проверка требует окружения/секретов/интеграций — запросить запуск у пользователя и указать, что проверить.",
            "rules": [
                "Давать точные шаги запуска (команда, env, порт).",
                "Просить прислать конкретные артефакты: логи, HTTP-ответы, скриншоты Swagger, дамп ошибок.",
                "Перечислять контрольные запросы (endpoint + ожидаемый результат)."
            ]
        }
    ],
    "nestSpecificRecommendations": [
        {
            "id": "controllers-are-adapters",
            "title": "Контроллеры как адаптеры",
            "description": "Контроллер не должен содержать бизнес-правила.",
            "rules": [
                "Контроллер валидирует вход, вызывает use-case и маппит ServiceResponse в HTTP-ответ.",
                "ServiceResponse.success=false превращать в предсказуемую HTTP-ошибку (через маппер ошибок), а не throw внутри домена."
            ]
        },
        {
            "id": "pipes-guards-filters",
            "title": "Граница ответственности NestJS",
            "description": "Валидация/авторизация/ошибки должны жить в соответствующих слоях NestJS.",
            "rules": [
                "Pipes — для парсинга/валидации входов (включая Zod).",
                "Guards — для auth/authz (без доменной логики).",
                "Filters — для унификации ошибок инфраструктуры и unexpected exceptions."
            ]
        }
    ]
}